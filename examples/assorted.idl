module foo {
    rpc void foo(projection call_proj* c) {
        projection <struct net_device> call_proj {
            rpc_ptr read [out] read;
        }
    }

    rpc_ptr long read(projection file *file) {
       projection <struct file> file {
           rpc_ptr read [out] read;
       }
    }
}
